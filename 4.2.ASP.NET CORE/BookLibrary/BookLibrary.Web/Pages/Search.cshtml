@page 
@model BookLibrary.Web.Pages.SearchModel
@{
    ViewData["Title"] = "Search";
}

<h2>Search results: "@Model.SearchTerm"</h2>
<ul>
    @foreach (var result in @Model.Results)
    {
        <li><a href="@(result.Url)">@result.Name</a> (@result.Type)</li>
    }
</ul>

